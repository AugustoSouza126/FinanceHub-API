spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/financehub
    username: postgres
    password: 123456

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

logging:
  level:
    org.springframework.boot.context.config: INFO