spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/financehub
        username: financehub
        password: financehub

    jpa:
        hibernate:
            ddl-auto: validate

        properties:
            hibernate:
                format_sql: true

        show-sql: true

    flyway:
        enabled: true
